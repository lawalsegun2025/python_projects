import sys 
import os
from PIL import Image 

# Grab the first and second arguments

# check if new/ exists, if not, create it

# loop through the first argument (Pokedex), 
# convert images to png